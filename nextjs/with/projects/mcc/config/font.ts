import { Luckiest_Guy as FontTitle, Livvic as FontSans } from "next/font/google";

export const fontTitle = FontTitle({
    weight: ['400'],
    subsets: ['latin']
});

export const fontSans = FontSans({
    weight: ['300'],
    subsets: ['latin']
});